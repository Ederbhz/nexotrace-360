services:
  - type: web
    name: nexotrace-360
    runtime: node
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    startCommand: pnpm start
    pullRequestPreviewsEnabled: true
    envVars:
      - key: PORTAL_TRANSPARENCIA_API_KEY
        sync: false
